org.gradle.jvmargs=-Xmx4G
#android.enableR8=true # 升级至AGP 8.13.0 后，android.enableR8 配置已废弃并移除，R8 已由 Android Gradle Plugin 默认启用，无需手动配置
android.useAndroidX=true
android.enableJetifier=true

#是否启用kotlin增量编译，默认true,设置为 false后 禁用增量编译，每次构建都会重新编译所有 Kotlin 代码（全量编译）
#kotlin.incremental=false
